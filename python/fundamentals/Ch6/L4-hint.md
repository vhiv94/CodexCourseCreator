# Hints — Validate Loaded Records

- Decide what a "valid line" must contain before writing code.
- Split once, inspect part count, and verify each piece has meaningful content.
- Keep validation strict enough to protect data quality, but simple enough to explain.
- Return booleans consistently so callers can filter lines cleanly.
