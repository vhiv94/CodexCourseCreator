# Hints — Dispatch Basic Commands

- Normalize incoming command text before comparisons.
- Decide what your function should return for supported commands first.
- Start with explicit branches for `add`, `list`, and `summary`; refactor later only if needed.
- Keep the function focused on routing, not full command execution.
