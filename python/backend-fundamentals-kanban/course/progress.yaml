last_completed: null
next_target: Ch1/L1
current_target:
  type: chapter
  ref: Ch1
  notes: The pre-Ch1 readiness checkpoint is complete. Route to the live Chapter 1 batch under course/M1/Ch1/, and keep the chapter review-heavy on HTTP request-response semantics, JSON response contracts, nested resource ownership, status and error categories, and SQLite's role in persistence.
generation_mode: chapter_batch
recent_assessment:
  kind: checkpoint
  summary: The learner is ready to begin the real Chapter 1 batch, but still needs explicit review on what requests and responses mean, why JSON is the API default, how nested routes express ownership, how common success and error statuses differ, and what SQLite is persisting for the project.
  review:
    - HTTP request and response semantics, especially what a health check is actually asking for
    - why JSON is the default machine-readable API format in this course
    - route design and resource ownership, especially /boards versus /boards/{board_id}/columns
    - status and error semantics such as 201, 405, 400, and 422
    - SQLite's role as the persisted datastore behind successful writes
  deepen:
    - FastAPI architecture
    - end-to-end backend troubleshooting
    - deployment and operations reasoning
  recommended_next_step: Start the live Chapter 1 lesson batch now, use it as guided review on the assessment weak spots, and reassess before moving into Chapter 2.
notes: |
  The refreshed intake confirms that Kanban Lite API is still the right capstone.
  The learner's Python base is strong enough that the course should not collapse into syntax-first review, but HTTP/API and data-modeling foundations still need explicit early depth.
  The three-module spine remains appropriate because foundations, application/data design, and runtime/operations are still meaningfully distinct phases.
  The chapter-local readiness checkpoint at course/M1/Ch1/ASSESSMENT.md is now complete and should inform pacing rather than block routing.
  Keep the logical Chapter 1 ids and selectors unchanged: Ch1/L1 through Ch1/L4 and lesson_ch1_l1 through lesson_ch1_l4.
  Content generation remains chapter-batched, starting with the live Chapter 1 batch and reassessing before Chapter 2.
  Chapter 1 should stay especially explicit about request meaning, JSON contracts, nested resource ownership, status-code categories, and SQLite's persistence role.
